simple Delay_channel
{
        parameters:
        @display("i=block/routing"); // add a default icon
    gates:
        input in;
        output out;
        input delay_med_in;
        output delay_med_out;
}
